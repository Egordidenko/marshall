import {
  Swiper,
  // Navigation,
  Pagination,
  // Controller,
  EffectFade,
  // Lazy,
  Autoplay,
  Parallax,
} from 'swiper/dist/js/swiper.esm';

Swiper.use([
  // Navigation,
  Pagination,
  // Controller,
  EffectFade,
  // Lazy,
  Autoplay,
  Parallax,
]);

export default Swiper;
